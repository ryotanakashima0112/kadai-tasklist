<h1>id:
<%= @task.id %>
の編集ページです
</h1>

<%= form_for(@task) do |f| %>

  <%= f.label :content, 'タスク'%>
  
  <%= f.text_field :content %>
  
  <%= f.label :status, "状況"%>
  
  <%= f.text_field :status %>
  
  <%= f.submit '投稿' %>
  
<% end %>