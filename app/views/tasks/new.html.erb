<h1>新規作成ページ</h1>

<%= form_for(@task) do |f| %>

  <%= f.label :content, 'タスク'%>
  
  <%= f.text_field :content %>
  
  <%= f.label :status, "状況"%>
  
  <%= f.text_field :status %>
  
  <%= f.submit '投稿' %>
  
<% end %>

<%= link_to '一覧ページに戻る', root_path %>